export class Product {
  constructor(public name: string, public description: string, public id: string, public created: string, public price: Object, public commodity_type: string, public pic?: any) {}
}
